import { Bildirimler } from "@/components/yonetim/bildirimler";

export const metadata = { title: "Bildirimler — LookBeds Yönetim" };

export default function YonetimBildirimler() {
  return <Bildirimler />;
}
