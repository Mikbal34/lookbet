import { Kullanicilar } from "@/components/yonetim/kullanicilar";

export const metadata = { title: "Kullanıcılar — LookBeds Yönetim" };

export default function YonetimKullanicilar() {
  return <Kullanicilar />;
}
